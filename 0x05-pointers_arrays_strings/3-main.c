#include "main.h"
#include <stdio.h>

/**
 *main - check the code
 *
 *Return: Always 0.
 */
int mai(void)
{
	char *str;

	str = "I do not fear computers, I fear the lack of them - Isaac Asimov";
	_put(str);
	return (0);
}

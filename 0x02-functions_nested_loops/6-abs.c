#include "main.h"

/**
 *_abs-prints the absolute value of an integer
 *@i: number targeted
 *Return: Always 0 (Success)
 */
int _abs(int i)
{
	if (i > 0)
	return (i);
	else if (i < 0)
	return (-1);
	else
	return (0);
}
